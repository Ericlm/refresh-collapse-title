import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'refresh-collapse-title',
  webDir: 'dist'
};

export default config;
